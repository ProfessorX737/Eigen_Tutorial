#include <iostream>
#include <conio.h>
#include <vector>

#include <Eigen/Dense>

using namespace std;
using Eigen::MatrixXd;
/*
int main(int argc, char * argv[])
{
	// MatrixXd matrix type represents a matrix of abritary size. (hence the X in MatrixXd)
	// the d means that every entry is a double
	MatrixXd m(2, 2);
	// Specifies a matrix with 2 rows and 2 columns

	m(0, 0) = 3; // top left corner
	// first number specifies the row index, second number is the column
	m(1, 0) = 2.5;
	m(0, 1) = -1;
	m(1, 1) = m(1, 0) + m(0, 1);
	cout << m << endl;

	_getch();

	return 0;
}
*/